---
id: web-dev
title: Web Development Notes
sidebar_position: 7
---

import { useState, useEffect } from 'react';
import { FaBookmark, FaRegBookmark } from 'react-icons/fa';

export function BookmarkButton({ id }) {
  const [bookmarked, setBookmarked] = useState(false);

  useEffect(() => {
    const saved = localStorage.getItem(`bookmark-${id}`);
    if (saved === 'true') setBookmarked(true);
  }, [id]);

  const toggleBookmark = () => {
    const newState = !bookmarked;
    setBookmarked(newState);
    localStorage.setItem(`bookmark-${id}`, newState);
  };

  return (
    <span
      onClick={toggleBookmark}
      style={{
        position: 'absolute',
        top: 10,
        right: 10,
        cursor: 'pointer',
        color: bookmarked ? '#f39c12' : '#999',
      }}
    >
      {bookmarked ? <FaBookmark size={20} /> : <FaRegBookmark size={20} />}
    </span>
  );
}

export const notesMeta = [
  { id: 'webd-html', title: 'HTML', subject: 'Web Development', pdf: '/pdfs/webd/html.pdf' },
  { id: 'webd-css', title: 'CSS', subject: 'Web Development', pdf: '/pdfs/webd/css.pdf' },
  { id: 'webd-javascript', title: 'Javascript', subject: 'Web Development', pdf: '/pdfs/webd/javascript.pdf' },
];

export default function WebDevPage() {
  return (
    <div>
      <h2>🌐 Chapter-wise Web Development Notes</h2>

      <div style={{ display: 'flex', flexWrap: 'wrap', gap: '1rem' }}>
        <div style={{ border: '1px solid #ccc', borderRadius: 10, padding: '1rem', width: 250, background: '#f9f9f9', boxShadow: '2px 2px 8px rgba(0,0,0,0.1)', position: 'relative' }}>
          <BookmarkButton id="webd-html" />
          <h3>🌐 HTML</h3>
          <p>HTML notes</p>
          <a href="/pdfs/webd/html.pdf" download>📥 Download PDF</a>
        </div>

        <div style={{ border: '1px solid #ccc', borderRadius: 10, padding: '1rem', width: 250, background: '#f9f9f9', boxShadow: '2px 2px 8px rgba(0,0,0,0.1)', position: 'relative' }}>
          <BookmarkButton id="webd-css" />
          <h3>🌐 CSS</h3>
          <p>CSS notes</p>
          <a href="/pdfs/webd/css.pdf" download>📥 Download PDF</a>
        </div>

        <div style={{ border: '1px solid #ccc', borderRadius: 10, padding: '1rem', width: 250, background: '#f9f9f9', boxShadow: '2px 2px 8px rgba(0,0,0,0.1)', position: 'relative' }}>
          <BookmarkButton id="webd-javascript" />
          <h3>🌐 Javascript</h3>
          <p>Javascript notes</p>
          <a href="/pdfs/webd/javascript.pdf" download>📥 Download PDF</a>
        </div>
      </div>
    </div>
  );
}
